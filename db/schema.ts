/**
 * Drizzle schema. Real tables (users, employers, jobs, stations,
 * job_stations, etc.) land in T2 (schema & station seed) — this file
 * exists now so drizzle-kit and the db client have a valid entry point to
 * wire against without blocking on the full schema design.
 */
export {}
