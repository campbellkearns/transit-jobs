# Postgres connection string. The database must have PostGIS available —
# `npm run db:setup` enables the extension and then pushes the schema.
# Neon (the target host) ships PostGIS on every tier.
DATABASE_URL=
